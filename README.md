# -2-tri---Atividade-# Solicita um número inteiro positivo ao usuário
X = int(input("Digite um número: "))

# Utiliza o laço while para imprimir os números de X até 0
while X >= 0:
    print(X)
    X -= 1
3---Python